<!--begin::sidebar menu-->
<div class="app-sidebar-menu overflow-hidden flex-column-fluid">
    <!--begin::Menu wrapper-->
    <div id="kt_app_sidebar_menu_wrapper" class="app-sidebar-wrapper hover-scroll-overlay-y my-5" data-kt-scroll="true"
         data-kt-scroll-activate="true" data-kt-scroll-height="auto"
         data-kt-scroll-dependencies="#kt_app_sidebar_logo, #kt_app_sidebar_footer"
         data-kt-scroll-wrappers="#kt_app_sidebar_menu" data-kt-scroll-offset="5px" data-kt-scroll-save-state="true">
        <!--begin::Menu-->
        <div class="menu menu-column menu-rounded menu-sub-indention px-3" id="#kt_app_sidebar_menu" data-kt-menu="true"
             data-kt-menu-expand="false">

            <!--begin:Menu item-->
            <div class="menu-item pt-5">
                <!--begin:Menu content-->
                <div class="menu-content">
                    <span class="menu-heading fw-bold text-uppercase fs-7">{# Le zoo #}</span>
                </div>
                <!--end:Menu content-->
            </div>

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_EMPLOYE') or is_granted('ROLE_VETO') %}
                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_animal_food_index') }}"
                       href="{{ path('app_admin_animal_food_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Rapport alimentation</span>
                    </a>
                </div>
            {% endif %}

           {# {% if  is_granted('ROLE_VETO') %}
                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_animal_index') }}"
                       href="{{ path('app_admin_animal_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Rapport alimentation(2)</span>
                    </a>
                </div>
            {% endif %}#}

            {% if is_granted('ROLE_ADMIN')  or is_granted('ROLE_VETO') or is_granted('ROLE_EMPLOYE') %}
                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_medical_report_index') }}"
                       href="{{ path('app_admin_medical_report_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Rapports médicaux</span>
                    </a>
                </div>
            {% endif %}

            {% if is_granted('ROLE_ADMIN')  or is_granted('ROLE_VETO') %}
                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_habitat_report_index') }}"
                       href="{{ path('app_admin_habitat_report_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Rapports sur les habitats</span>
                    </a>
                </div>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_user_index') }}"
                       href="{{ path('app_admin_user_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Utilisateurs</span>
                    </a>
                </div>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <div class="menu-item pt-5">
                    <div class="menu-content">
                        <span class="menu-heading fw-bold text-uppercase fs-7">Datas</span>
                    </div>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_animal_index') }}"
                       href="{{ path('app_admin_animal_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Animaux</span>
                    </a>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_food_index') }}"
                       href="{{ path('app_admin_food_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Aliments </span>
                    </a>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_race_index') }}"
                       href="{{ path('app_admin_race_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Races</span>
                    </a>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_habitat_index') }}"
                       href="{{ path('app_admin_habitat_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Habitats</span>
                    </a>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_health_status_index') }}"
                       href="{{ path('app_admin_health_status_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Etats de santés</span>
                    </a>
                </div>
            {% endif %}

            {% if is_granted('ROLE_ADMIN')  or is_granted('ROLE_EMPLOYE') %}
                <div class="menu-item pt-5">
                    <div class="menu-content">
                        <span class="menu-heading fw-bold text-uppercase fs-7">Contenu Web</span>
                    </div>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_review_index') }}"
                       href="{{ path('app_admin_review_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Avis visiteurs</span>
                    </a>
                </div>

                <div class="menu-item">
                    <a class="menu-link {{ is_current_route_return_active('app_admin_service_index') }}"
                       href="{{ path('app_admin_service_index') }}">
                        <span class="menu-icon">{{ theme.getIcon('address-book', 'fs-2') | raw }}</span>
                        <span class="menu-title">Services du zoo</span>
                    </a>

                </div>
            {% endif %}

            <!--begin:Menu item-->
            <div class="menu-item pt-5">
                <!--begin:Menu content-->
                <div class="menu-content">
                    <span class="menu-heading fw-bold text-uppercase fs-7">Aide</span>
                </div>
                <!--end:Menu content-->
            </div>
            <!--end:Menu item-->
            <!--begin:Menu item-->
            <div class="menu-item">

                <a class="menu-link" href="#">
                    <span class="menu-icon">{{ theme.getIcon('rocket', 'fs-2') | raw }}</span>
                    <span class="menu-title">Support</span>
                </a>

            </div>
            <!--end:Menu item-->
            <!--begin:Menu item-->
            <div class="menu-item">

                <a class="menu-link" href="#">
                    <span class="menu-icon">{{ theme.getIcon('abstract-26', 'fs-2') | raw }}</span>
                    <span class="menu-title">Documentation</span>
                </a>

            </div>
            <!--end:Menu item-->
            <!--begin:Menu item-->
            <div class="menu-item">

                <a class="menu-link" href="#" target="_blank">
                    <span class="menu-icon">{{ theme.getIcon('code', 'fs-2') | raw }}</span>
                    <span class="menu-title">Version</span>
                </a>

            </div>
            <!--end:Menu item-->
        </div>
        <!--end::Menu-->
    </div>
    <!--end::Menu wrapper-->
</div>
<!--end::sidebar menu-->
